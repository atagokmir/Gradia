name: Deploy

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Docker Hub'a login
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_TOKEN }}

      - name: gradia image build ve push
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: atacetinel/gradia:latest

      - name: gradia-nginx image build ve push
        uses: docker/build-push-action@v5
        with:
          context: ./nginx
          push: true
          tags: atacetinel/gradia-nginx:latest
